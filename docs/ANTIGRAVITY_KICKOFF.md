# Antigravity kickoff — Lamma standalone

You are in `/Users/mostafamohamedelazab/Documents/lamma`, a brand-new React Native CLI app. Read `docs/REBUILD_PLAN.md`.

Hard rules: Google/Apple/Anonymous auth only — never phone/OTP. Standalone — no imports from dawwar. Pixel-match `design-reference/`. Use only files under `src/assets/`.

## Phase 1 (use Claude Opus)
Build `src/design-system` completely: tokens + atoms + molecules + organisms + ScreenTemplate. Wire asset requires. EN/AR i18n stubs + RTL helpers. Commit message: `phase1: design system + tokens + assets`. Push to `origin/main`.

Then continue Phase 2 without waiting.
