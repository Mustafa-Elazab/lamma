// Single source of truth lives in the app bundle so Firestore and the
// offline fallback stay identical.
const quarterMilePacks = require('../src/features/games/quarter-mile/content/quarterMilePacks.json');

module.exports = { quarterMilePacks };
